services:
  api:
    command: ["bash", "/app/startup.sh", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    volumes:
      - .:/app

  celery-worker:
    volumes:
      - .:/app

  celery-beat:
    volumes:
      - .:/app

  flower:
    volumes:
      - .:/app
