fastapi>=0.115.0
uvicorn[standard]==0.24.0
sqlalchemy>=2.0.36
alembic==1.12.1
psycopg2-binary>=2.9.9,<3.0.0
pgvector>=0.2.4
python-jose[cryptography]==3.3.0
bcrypt>=4.0.0
python-multipart==0.0.6
pydantic>=2.9.0
pydantic-settings>=2.6.0
email-validator>=2.0.0
requests==2.31.0
numpy>=1.26.2
torch>=2.0.0
# Dockerfile installs the CPU torch build with --no-deps (skips ~900MB of CUDA
# libs), which also drops these pure-Python transitive deps that torch still
# needs at runtime (e.g. torch.optim triggers torch._dynamo -> sympy). List
# them explicitly so both CPU and GPU builds get them.
filelock
fsspec
sympy
jinja2
networkx>=2.5.1
celery[redis]==5.3.4
redis>=4.5.2,<5.0.0
flower==2.0.1
soundfile>=0.12
librosa>=0.10.2
matplotlib==3.8.2
setuptools>=65.0.0
pytest
tensorflow
httpx>=0.24.0
tenacity>=8.0.0
scikit-learn>=1.3.0
scikit-learn>=1.3.0
umap-learn>=0.5.5
faiss-cpu
psutil>=5.9